package org.wildfly.clustering.server.infinispan.provider;

import "org.jgroups.stack.proto";
import "org.jgroups.util.proto";
import "org.infinispan.remoting.transport.jgroups.proto";

// IDs: 140 -144

/**
 * @TypeId(141)
 */
message ConcurrentAddressSetAddFunction {
	optional	org.jgroups.util.UUID	uuidAddress	= 1;
	optional	org.jgroups.stack.IpAddress	ipAddress	= 2;
}

/**
 * @TypeId(142)
 */
message ConcurrentAddressSetRemoveFunction {
	optional	org.jgroups.util.UUID	uuidAddress	= 1;
	optional	org.jgroups.stack.IpAddress	ipAddress	= 2;
}

/**
 * @TypeId(143)
 */
message CopyOnWriteAddressSetAddFunction {
	optional	org.jgroups.util.UUID	uuidAddress	= 1;
	optional	org.jgroups.stack.IpAddress	ipAddress	= 2;
}

/**
 * @TypeId(144)
 */
message CopyOnWriteAddressSetRemoveFunction {
	optional	org.jgroups.util.UUID	uuidAddress	= 1;
	optional	org.jgroups.stack.IpAddress	ipAddress	= 2;
}

/**
 * @TypeId(145)
 */
message AddressSetAddFunction {
	repeated	org.infinispan.remoting.transport.jgroups.JGroupsAddress	address	= 1;
}

/**
 * @TypeId(146)
 */
message AddressSetRemoveFunction {
	repeated	org.infinispan.remoting.transport.jgroups.JGroupsAddress	address	= 1;
}
