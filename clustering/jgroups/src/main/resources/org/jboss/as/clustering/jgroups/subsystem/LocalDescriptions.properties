jgroups=The configuration of the jgroups subsystem.
jgroups.add=Add the jgroups subsystem.
jgroups.remove=Remove the jgroups subsystem.
jgroups.describe=Describe the jgroups subsystem.
jgroups.default-stack=The default jgroups protocol stack.
jgroups.stack=A JGroups stack child.
# stack resource
stack=The configuration of a jgroups protocol stack.
stack.add=Add a protocol stack to the jgroups subsystem.
stack.add.transport=Optional OBJECT parameter specifying the transport for the stack.
stack.add.transport.type=The implementation class for a protocol, which determines protocol functionality.
stack.add.transport.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
stack.add.transport.properties=A list of properties for a protocol layer. Used to customize protocol behaviour.
stack.add.transport.shared=If true, the underlying transport is shared by all channels using this stack.
stack.add.transport.diagnostics-socket-binding=The diagnostics socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
stack.add.transport.default-executor=The thread pool executor to handle incoming messages.
stack.add.transport.oob-executor=The thread pool executor to handle incoming out-of-band messages.
stack.add.transport.timer-executor=The thread pool executor to handle protocol-related timing tasks.
stack.add.transport.thread-factory=The thread factory to use for handling asynchronous transport-specific tasks.
stack.add.transport.site=The site (i.e. data centre) identifier for this node. Used by Infinispan topology-aware consistent hash.
stack.add.transport.rack=The rack (i.e. server rack) identifier for this node. Used by Infinispan topology-aware consistent hash.
stack.add.transport.machine=The machine (i.e. host) identifier for this node. Used by Infinispan topology-aware consistent hash.
stack.add.transport.property=A JGroups transport property.
stack.add.protocol.type=The implementation class for a protocol, which determines protocol functionality.
stack.add.protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
stack.add.protocol.properties=Optional LIST parameter specifying the protocol list for the stack.
stack.add.protocol.property=A JGroups protocol property.
stack.add.protocols=Optional LIST parameter specifying the protocol list for the stack.
stack.remove=Remove a protocol stack from the jgroups subsystem.
stack.add-protocol=Add the configuration of a protocol within a protocol stack.
stack.add-protocol.type=The implementation class for a protocol, which determines protocol functionality.
stack.add-protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
stack.add-protocol.properties=Optional LIST parameter specifying the protocol list for the stack.
stack.add-protocol.property=A JGroups protocol property.
stack.remove-protocol=Remove the configuration of a protocol within a protocol stack.
stack.remove-protocol.type=The implementation class for a protocol, which determines protocol functionality.
stack.export-native-configuration=Export this stack configuration as a native JGroups format.
stack.name=The name for a protocol stack.
stack.transport=A JGroups stack transport layer.
stack.protocol=A JGroups stack protocol layer.
stack.protocols=The list of configured protocols for a protocol stack.
# transport resource
transport=The configuration of a transport for a protocol stack.
transport.add=Add the configuration of a transport for a protocol stack.
transport.remove=Remove the configuration of a transport for a protocol stack.
transport.type=The implementation class for a protocol, which determines protocol functionality.
transport.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
transport.shared=If true, the underlying transport is shared by all channels using this stack.
transport.diagnostics-socket-binding=The diagnostics socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
transport.default-executor=The thread pool executor to handle incoming messages.
transport.oob-executor=The thread pool executor to handle incoming out-of-band messages.
transport.timer-executor=The thread pool executor to handle protocol-related timing tasks.
transport.thread-factory=The thread factory to use for handling asynchronous transport-specific tasks.
transport.site=The site (i.e. data centre) identifier for this node. Used by Infinispan topology-aware consistent hash.
transport.rack=The rack (i.e. server rack) identifier for this node. Used by Infinispan topology-aware consistent hash.
transport.machine=The machine (i.e. host) identifier for this node. Used by Infinispan topology-aware consistent hash.
transport.properties=Optional LIST parameter specifying the property list for the transport.
transport.property=A JGroups transport property.
# protocol resource
protocol=The configuration of a protocol within a protocol stack.
protocol.type=The implementation class for a protocol, which determines protocol functionality.
protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
protocol.properties=An optional list of protocol properties.
protocol.property=A JGroups protocol property.
# property resource
property=A protocol property with name and value.
property.add=Adds a protocol property.
property.remove=Removes a protocol property.
property.value=The value of a protocol property.


