<?xml version="1.0"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.wildfly</groupId>
        <artifactId>wildfly-parent</artifactId>
        <version>10.0.0.Alpha2-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>com.redhat.gss</groupId>
    <artifactId>wildfly-redhat-support-lib</artifactId>
    <name>WildFly: Redhat Support Library</name>
    <dependencies>
        <dependency>
            <groupId>org.jboss.logging</groupId>
            <artifactId>jboss-logging</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>commons-net</groupId>
            <artifactId>commons-net</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>resteasy-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>resteasy-multipart-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.spec.javax.ws.rs</groupId>
            <artifactId>jboss-jaxrs-api_2.0_spec</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>javax.mail</artifactId>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>commons-net</groupId>
                <artifactId>commons-net</artifactId>
                <version>${version.commons-net}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${version.checkstyle.plugin}</version>
                    <configuration>
                        <sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>jaxb2-maven-plugin</artifactId>
                <version>2.1</version>
                <executions>
                    <execution>
                        <id>xjc</id>
                        <goals>
                            <goal>xjc</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <sources>
                        <source>src/main/xsd/strata.xsd</source>
                    </sources>
                    <encoding>UTF-8</encoding>
                    <packageName>com.redhat.gss.redhat_support_lib.parsers</packageName>
                    <extension>true</extension>
                </configuration>
            </plugin>

            <!-- todo remove anything that smells of ant-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>build-dist</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <target>
                                <!-- Replace the '.' in ${module.name} with '/' to get its path -->
                                <tempfile property="temp.file"/>
                                <echo message="${module.name}" file="${temp.file}"/>
                                <replace file="${temp.file}" token="." value="/"/>
                                <loadfile srcfile="${temp.file}" property="module.path"/>
                                <delete file="${temp.file}"/>

                                <delete dir="target/module"/>
                                <property name="module.dir" value="target/module/${module.path}/main"/>
                                <property name="wildfly_dir"
                                          value="../build/target/${server.output.dir.prefix}-${jboss.as.release.version}/modules/system/layers/base/com/redhat/gss/redhat-support-lib/main"/>
                                <property name="dist_dir"
                                          value="../dist/target/${server.output.dir.prefix}-${jboss.as.release.version}/modules/system/layers/base/com/redhat/gss/redhat-support-lib/main"/>

                                <copy file="src/main/resources/module/main/module.xml"
                                      tofile="${module.dir}/module.xml"/>
                                <copy file="target/${project.artifactId}.jar" tofile="${module.dir}/jboss-redhat-support-lib.jar"/>

                                <copy file="src/main/resources/module/main/module.xml"
                                      tofile="${wildfly_dir}/module.xml"/>
                                <copy file="target/${project.artifactId}.jar" tofile="${wildfly_dir}/jboss-redhat-support-lib.jar"/>

                                <copy file="src/main/resources/module/main/module.xml"
                                      tofile="${dist_dir}/module.xml"/>
                                <copy file="target/${project.artifactId}.jar" tofile="${dist_dir}/jboss-redhat-support-lib.jar"/>

                                <echo>Module ${module.name} has been created in the
                                    target/module directory. Copy to your WildFly installation.
                                </echo>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <finalName>wildfly-redhat-support-lib</finalName>
    </build>
</project>