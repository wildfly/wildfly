jca=The connector subsystem providing general config for JCA connectors
archive-validation=Archive validation configs for deployed connectors' rar
archive-validation.enabled=Enabling the validation
archive-validation.fail-on-error=Should an archive validation error report fail the deployment. Default: true
archive-validation.fail-on-warn=Should an archive validation warning report fail the deployment. Default: false
bean-validation=Toggle bean validation (JSR-303) for the deployment units.
bean-validation.enabled=Specify whether bean validation is enabled
threadpool=threads config for workmanager
cached-connection-manager.debug=enable/disable debug information logging for cached connection manager
cached-connection-manager.error=enable/disable error information logging for cached connection manager
connector.add=Adds connector
connector.archive-validation.add=Adds archive validation
connector.archive-validation.enabled=Enabling the validation
connector.archive-validation.fail-on-error=Should an archive validation error report fail the deployment
connector.archive-validation.fail-on-warn=Should an archive validation warning report fail the deployment
connector.archive-validation.remove=Removes archive validation
connector.default-workmanager.add=Adds configurations for thread pools used by default workmanager
connector.default-workmanager.short-running-thread-pool=Specify the name of short running thread pool
connector.default-workmanager.long-running-thread-pool=Specify the name of long running thread pool
connector.default-workmanager.remove=Removes configurations for thread pools used by default workmanager
