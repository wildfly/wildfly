jca=The Java EE Connector Architecture (JCA) subsystem providing general configuration for resource adapters
jca.add=Add the JCA subsystem
jca.remove=Remove the JCA subsystem
jca.archive-validation=Archive validation for resource adapters
jca.archive-validation.add=Add archive validation functionality
jca.archive-validation.remove=Remove archive validation functionality
jca.archive-validation.enabled=Specify whether archive validation is enabled
jca.archive-validation.fail-on-error=Should an archive validation error report fail the deployment
jca.archive-validation.fail-on-warn=Should an archive validation warning report fail the deployment
jca.bean-validation=Bean validation (JSR-303) for resource adapters
jca.bean-validation.add=Add bean validation functionality
jca.bean-validation.remove=Remove bean validation functionality
jca.bean-validation.enabled=Specify whether bean validation is enabled
jca.tracer=Tracer for resource adapters
jca.tracer.add=Add tracer config functionality
jca.tracer.remove=Remove tracer config  functionality
jca.tracer.enabled=Specify whether tracer is enabled
jca.bootstrap-context=Bootstrap context for resource adapters
jca.bootstrap-context.add=Add BootstrapContext
jca.bootstrap-context.remove=Remove BootstrapContext
jca.bootstrap-context.name=The name of the BootstrapContext
jca.bootstrap-context.workmanager=The WorkManager instance for the BootstrapContext
jca.cached-connection-manager=Cached connection manager for resource adapters
jca.cached-connection-manager.add=Add cached connection manager functionality
jca.cached-connection-manager.remove=Remove cached connection manager functionality
jca.cached-connection-manager.debug=Enable/disable debug information logging
jca.cached-connection-manager.error=Enable/disable error information logging
jca.cached-connection-manager.install=Enable/disable the cached connection manager valve and interceptor
jca.cached-connection-manager.ignore-unknown-connections=Do not cache unknown connections
jca.cached-connection-manager.get-number-of-connections=Get the number of connections currently registered with the cache connection manager
jca.cached-connection-manager.list-connections=List the connections currently registered with the cache connection manager
jca.workmanager=WorkManager for resource adapters
jca.workmanager.add=Add work manager
jca.workmanager.remove=Remove work manager
jca.workmanager.name=The name of the WorkManager
jca.workmanager.short-running=The thread pool for short running jobs
jca.workmanager.long-running=The thread pool for long running jobs

jca.workmanager.long-running-threads=boolean indicating if service for long running activated
jca.workmanager.short-running-threads=boolean indicating if service for short running activated


jca.distributed-workmanager=DistributedWorkManager for resource adapters
jca.distributed-workmanager.add=Add distributed work manager
jca.distributed-workmanager.remove=Remove distributed work manager
jca.distributed-workmanager.name=The name of the DistributedWorkManager
jca.distributed-workmanager.policy=The policy decides when to redistribute a Work instance
jca.distributed-workmanager.policy-options=List of policy's options key/value pairs
jca.distributed-workmanager.selector=The selector decides to which nodes in the network to redistribute the Work instance to
jca.distributed-workmanager.selector-options=List of selector's options key/value pairs
jca.distributed-workmanager.short-running=The thread pool for short running jobs
jca.distributed-workmanager.long-running=The thread pool for long running jobs
jca.distributed-workmanager.transport-jgroups-stack=Define which jgroups stack jca will use for distribute work in the cluster. Default is udp
jca.distributed-workmanager.transport-jgroups-cluster=Define jgroups cluster name jca will use for distribute work in the cluster. Default is jca
jca.distributed-workmanager.transport-request-timeout=Define timeout for works' distribution in the cluster. It is expressed in milliseconds. Default is 10000

jca.distributed-workmanager.long-running-threads=boolean indicating if service for long running activated
jca.distributed-workmanager.short-running-threads=boolean indicating if service for short running activated
