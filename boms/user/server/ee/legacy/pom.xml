<?xml version='1.0' encoding='UTF-8'?>
<!--
  ~ Copyright The WildFly Authors
  ~ SPDX-License-Identifier: Apache-2.0
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.wildfly.bom.builder</groupId>
        <artifactId>wildfly-server-ee</artifactId>
        <!--
        Maintain separation between the artifact id and the version to help prevent
        merge conflicts between commits changing the GA and those changing the V.
        -->
        <version>40.0.0.Final-SNAPSHOT</version>
    </parent>

    <packaging>pom</packaging>

    <artifactId>wildfly-server-ee-legacy-builder</artifactId>

    <name>WildFly BOM Builders: Server EE Legacy Builder</name>
    <description>This artifact builds a bill of materials (BOM) for WildFly Server EE Legacy's Dependency Management</description>

    <dependencyManagement>
        <dependencies>
            <!-- base import of server's dep management -->
            <dependency>
                <groupId>org.wildfly</groupId>
                <artifactId>wildfly-legacy-ee-bom</artifactId>
                <version>${ee.maven.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- include client BOMs -->
            <dependency>
                <groupId>org.wildfly</groupId>
                <artifactId>wildfly-ejb-client-bom</artifactId>
                <version>${ee.maven.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>org.wildfly</groupId>
                <artifactId>wildfly-jaxws-client-bom</artifactId>
                <version>${ee.maven.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>org.wildfly</groupId>
                <artifactId>wildfly-jms-client-bom</artifactId>
                <version>${ee.maven.version}</version>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- enforces all BOM artifacts are available -->
    <dependencies>
        <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-ee-10-feature-pack</artifactId>
            <version>${ee.maven.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>${channels.maven.groupId}</groupId>
            <artifactId>wildfly-ee-10</artifactId>
            <version>${ee.maven.version}</version>
            <type>pom</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.wildfly.plugins</groupId>
                <artifactId>wildfly-bom-builder-plugin</artifactId>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>build-bom</id>
                        <goals>
                            <goal>build-bom</goal>
                        </goals>
                        <configuration>
                            <bomParent>
                                <groupId>org.jboss</groupId>
                                <artifactId>jboss-parent</artifactId>
                                <relativePath/>
                            </bomParent>
                            <bomGroupId>${bomGroupId}</bomGroupId>
                            <bomArtifactId>${bomArtifactIdPrefix}-ee-10</bomArtifactId>
                            <bomVersion>${project.version}</bomVersion>
                            <bomName>${full.dist.product.release.name} BOMs: EE 10</bomName>
                            <bomDescription>${full.dist.product.release.name}: EE 10 Dependency Management</bomDescription>
                            <channels>
                                <channel>
                                    <manifest>
                                        <groupId>${channels.maven.groupId}</groupId>
                                        <artifactId>wildfly-ee-10</artifactId>
                                        <version>${version.boms.channel.ee}</version>
                                    </manifest>
                                </channel>
                            </channels>
                            <includeDependencies combine.children="append">
                                <!-- include here any <dependency /> exclusive to this BOM -->
                                <dependency>
                                    <groupId>org.jboss.spec.jakarta.el</groupId>
                                    <artifactId>jboss-el-api_5.0_spec</artifactId>
                                </dependency>
                                <dependency>
                                    <groupId>org.hibernate.orm</groupId>
                                    <artifactId>hibernate-jpamodelgen</artifactId>
                                </dependency>
                            </includeDependencies>
                            <versionRefDependencies combine.children="append">
                                <dependency>
                                    <groupId>org.hibernate.orm</groupId>
                                    <artifactId>hibernate-jpamodelgen</artifactId>
                                    <version>org.hibernate.orm:hibernate-core:jar</version>
                                </dependency>
                            </versionRefDependencies>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>with-tools</module>
    </modules>
</project>
