security-manager=Security Manager subsystem
security-manager.add=Operation Adds subsystem
security-manager.remove=Operation Removes subsystem
security-manager.deployment-permissions=The permissions that are to be granted to deployments
security-manager.deployment-permissions.add=Adds the deployment permissions
security-manager.deployment-permissions.remove=Removes the deployment permissions
security-manager.deployment-permissions.minimum-permissions=The minimum set of permissions to be granted to a deployment or jars.
security-manager.deployment-permissions.minimum-permissions.class=The permission fully-qualified class name
security-manager.deployment-permissions.minimum-permissions.name=The permission target name
security-manager.deployment-permissions.minimum-permissions.actions=The permission actions
security-manager.deployment-permissions.minimum-permissions.module=The module to load the permission from. If not specified it will be loaded using the class loader of the security manager subsystem.
security-manager.deployment-permissions.maximum-permissions=The maximum set of permissions that can be granted to a deployment or jars.
security-manager.deployment-permissions.maximum-permissions.class=The permission fully-qualified class name
security-manager.deployment-permissions.maximum-permissions.name=The permission target name
security-manager.deployment-permissions.maximum-permissions.actions=The permission actions
security-manager.deployment-permissions.maximum-permissions.module=The module to load the permission from. If not specified it will be loaded using the class loader of the security manager subsystem.
