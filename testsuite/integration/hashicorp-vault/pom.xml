<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright The WildFly Authors
  ~ SPDX-License-Identifier: Apache-2.0
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.wildfly</groupId>
        <artifactId>wildfly-ts-integ</artifactId>
        <version>41.0.0.Beta1-SNAPSHOT</version>
    </parent>

    <artifactId>wildfly-ts-integ-hashicorp-vault</artifactId>
    <name>WildFly Test Suite: Integration - HashiCorp Vault</name>

    <properties>
        <jbossas.ts.integ.dir>${basedir}/..</jbossas.ts.integ.dir>
        <jbossas.ts.dir>${jbossas.ts.integ.dir}/..</jbossas.ts.dir>
        <jbossas.project.dir>${jbossas.ts.dir}/..</jbossas.project.dir>
        <jboss.dist>${project.build.directory}/wildfly-layers</jboss.dist>
        <server.jboss.args>${jboss.args}</server.jboss.args>
        <hashicorp.vault.provisioning.phase>process-test-classes</hashicorp.vault.provisioning.phase>
        <version.hashicorp-vault.test.junit>6.0.0</version.hashicorp-vault.test.junit>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${version.hashicorp-vault.test.junit}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Execute published Vault tests directly in WildFly testsuite -->
        <dependency>
            <groupId>org.wildfly.security.vault</groupId>
            <artifactId>wildfly-vault-tests</artifactId>
            <version>${version.org.wildfly.security.vault}</version>
            <type>test-jar</type>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
        <!-- SubsystemJUnit5TestCase (vault tests jar) extends SubsystemTestCase from subsystem test utilities -->
        <dependency>
            <groupId>org.wildfly.security.vault</groupId>
            <artifactId>wildfly-vault-subsystem</artifactId>
            <version>${version.org.wildfly.security.vault}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit</groupId>
                    <artifactId>junit-bom</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.wildfly.security.vault</groupId>
            <artifactId>wildfly-vault-subsystem</artifactId>
            <version>${version.org.wildfly.security.vault}</version>
            <type>test-jar</type>
            <classifier>tests</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit</groupId>
                    <artifactId>junit-bom</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-vault</artifactId>
            <version>${version.org.testcontainers}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wildfly.core</groupId>
            <artifactId>wildfly-embedded</artifactId>
            <version>${version.org.wildfly.core}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wildfly.core</groupId>
            <artifactId>wildfly-protocol</artifactId>
            <version>${version.org.wildfly.core}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jboss.arquillian.junit5</groupId>
            <artifactId>arquillian-junit5-container</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.smallrye.certs</groupId>
            <artifactId>smallrye-certificate-generator</artifactId>
            <version>0.9.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.wildfly.plugins</groupId>
                <artifactId>wildfly-maven-plugin</artifactId>
                <version>${version.org.wildfly.plugin}</version>
                <configuration>
                    <overwrite-provisioned-server>true</overwrite-provisioned-server>
                    <log-provisioning-time>${galleon.log.time}</log-provisioning-time>
                    <offline-provisioning>${galleon.offline}</offline-provisioning>
                    <record-provisioning-state>false</record-provisioning-state>
                    <galleon-options>
                        <jboss-fork-embedded>${galleon.fork.embedded}</jboss-fork-embedded>
                        <optional-packages>passive+</optional-packages>
                    </galleon-options>
                </configuration>
                <executions>
                    <execution>
                        <id>hashicorp-vault-provisioning</id>
                        <goals>
                            <goal>provision</goal>
                        </goals>
                        <phase>${hashicorp.vault.provisioning.phase}</phase>
                        <configuration>
                            <provisioning-dir>${project.build.directory}/wildfly-layers</provisioning-dir>
                            <feature-packs>
                                <feature-pack>
                                    <groupId>${testsuite.full.feature.pack.groupId}</groupId>
                                    <artifactId>${testsuite.full.feature.pack.artifactId}</artifactId>
                                    <version>${testsuite.full.feature.pack.version}</version>
                                    <inherit-configs>false</inherit-configs>
                                    <inherit-packages>false</inherit-packages>
                                </feature-pack>
                            </feature-packs>
                            <layers>
                                <layer>base-server</layer>
                                <layer>elytron</layer>
                                <layer>management</layer>
                                <layer>core-tools</layer>
                                <layer>web-server</layer>
                                <layer>hashicorp-vault</layer>
                            </layers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <dependencies>
                    <!-- Prefer JUnit Platform over JUnit 4 (parent brings arquillian-junit-core) -->
                    <dependency>
                        <groupId>org.apache.maven.surefire</groupId>
                        <artifactId>surefire-junit-platform</artifactId>
                        <version>${version.surefire.plugin}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <dependenciesToScan>
                        <dependency>org.wildfly.security.vault:wildfly-vault-tests:test-jar:tests</dependency>
                    </dependenciesToScan>
                    <includes>
                        <include>**/*TestCase.java</include>
                    </includes>
                    <excludeJUnit5Engines>
                        <excludeJUnit5Engine>junit-vintage</excludeJUnit5Engine>
                    </excludeJUnit5Engines>
                    <systemPropertyVariables>
                        <jboss.home>${project.build.directory}/wildfly-layers</jboss.home>
                        <arquillian.launch>wildfly</arquillian.launch>
                        <server.jvm.args>${server.jvm.args}</server.jvm.args>
                        <server.jboss.args>${server.jboss.args}</server.jboss.args>
                        <module.path>${project.build.directory}/wildfly-layers/modules</module.path>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
