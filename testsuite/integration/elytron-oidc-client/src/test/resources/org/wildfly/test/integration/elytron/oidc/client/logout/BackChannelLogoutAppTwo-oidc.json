{
    "realm" : "WildFly",
    "resource" : "BackChannelLogoutAppTwo",
    "public-client" : "false",
    "auth-server-url" : "${logout.auth.server.url}",
    "ssl-required" : "EXTERNAL",
    "logout-path" : "${logout.path.value.prop}",
    "logout-callback-path" : "${logout.callback.path.value.prop}",
    "credentials" : {
        "secret" : "${client.secret.prop}"
    }
}
