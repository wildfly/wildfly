<?xml version='1.0' encoding='UTF-8'?>

<host xmlns="urn:jboss:domain:1.6" name="slave">

    <management>
        <security-realms>
            <security-realm name="ManagementRealm">
                <server-identities>
                     <!-- Replace this with either a base64 password of your own, or use a vault with a vault expression -->
                     <secret value="YWRtaW4="/>
                </server-identities>

                <authentication>
                    <local default-user="$local" skip-group-loading="true"/>
                    <properties path="mgmt-users.properties" relative-to="jboss.domain.config.dir"/>
                </authentication>
                <authorization map-groups-to-roles="false">
                    <properties path="mgmt-groups.properties" relative-to="jboss.domain.config.dir"/>
                </authorization>
            </security-realm>
            <security-realm name="ApplicationRealm">
                <authentication>
                    <local allowed-users="*" default-user="$local" skip-group-loading="true"/>
                    <properties path="application-users.properties" relative-to="jboss.domain.config.dir"/>
                </authentication>
                <authorization>
                    <properties path="application-roles.properties" relative-to="jboss.domain.config.dir"/>
                </authorization>
            </security-realm>
        </security-realms>
        <audit-log>
            <formatters>
                <json-formatter name="json-formatter"/>
            </formatters>
            <handlers>
                <file-handler formatter="json-formatter" name="host-file" path="audit-log.log" relative-to="jboss.domain.data.dir"/>
                <file-handler formatter="json-formatter" name="server-file" path="audit-log.log" relative-to="jboss.server.data.dir"/>
            </handlers>
            <logger enabled="false" log-boot="true" log-read-only="false">
                <handlers>
                    <handler name="host-file"/>
                </handlers>
            </logger>
            <server-logger enabled="false" log-boot="true" log-read-only="false">
                <handlers>
                    <handler name="server-file"/>
                </handlers>
            </server-logger>
        </audit-log>
        <management-interfaces>
            <native-interface security-realm="ManagementRealm">
                <socket interface="management" port="${jboss.management.native.port:19999}"/>
            </native-interface>
        </management-interfaces>
    </management>

    <domain-controller>
        <remote host="${jboss.test.host.master.address}" port="${jboss.domain.master.port:9999}" security-realm="ManagementRealm" username="admin"/>
    </domain-controller>

    <interfaces>
        <interface name="management">
            <inet-address value="${jboss.test.host.slave.address}"/>
        </interface>
        <interface name="public">
            <inet-address value="${jboss.test.host.slave.address}"/>
        </interface>
        <interface name="unsecure">
            <!-- Used for IIOP sockets in the standard configuration.
                 To secure JacORB you need to setup SSL -->
            <inet-address value="${jboss.test.host.slave.address}"/>
        </interface>
    </interfaces>

    <jvms>
        <jvm name="default">
            <heap max-size="256m" size="64m"/>
            <permgen max-size="256m" size="256m"/>
               <jvm-options>
                  <option value="-server"/>
              </jvm-options>
         </jvm>
    </jvms>

    <servers>
        <server auto-start="true" group="main-server-group" name="server-one">
            <socket-bindings port-offset="100"/>
        </server>
    </servers>
</host>
